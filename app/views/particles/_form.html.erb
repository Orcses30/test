<%= form_with(model: particle, local: true) do |form| %>
  <% if particle.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(particle.errors.count, "error") %> prohibited this particle from being saved:</h2>

      <ul>
      <% particle.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :particle_title %>
  </div>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_area :body, id: :particle_body %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
