<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @particle.title %>
</p>

<p>
  <strong>Body:</strong>
  <%= @particle.body %>
</p>

<%= link_to 'Edit', edit_particle_path(@particle) %> |
<%= link_to 'Back', particles_path %>
